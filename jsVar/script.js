// 1
const person = {
    firstName: "",
    surname: "",
    age: ""
};

// 2 
person.firstName = `Jesse`;
person.surname = `De Jong`;
person.age = `26`;

// 3
console.log(`Hi, my name is ${person.firstName} ${person.surname} and I am ${person.age} years old.`);